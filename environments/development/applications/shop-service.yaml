apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: shop-service
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/Amarjeetlocato/Dhondoo-To---gitops.git
    targetRevision: main
    path: helm/shop-service

    helm:
      valueFiles:
        - ../../environments/development/values/shop-service-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: platform

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true