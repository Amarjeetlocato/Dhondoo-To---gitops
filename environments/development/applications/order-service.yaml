apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: order-service
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/Amarjeetlocato/Dhondoo-To---gitops.git
    targetRevision: main
    path: helm/order-service

    helm:
      valueFiles:
        - ../../environments/development/values/order-service-values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: platform

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true